<% local_assigns[:event].each do |event| %>
  <div class="single-event">
    <%= image_tag(event.event_image_url, alt: event.title, class:"single-event__img") %>
    <div class="single-event__content">
      <h5><%= event.title.humanize %></h5>
      <p><%= event.address%></p>
      <p>Price: $ <%= event.price%></p>
    </div>
    <div class="single-event__buttons">
      <%= display_join_button(event, 'join-button') %>
      <div class="crud-buttons">
        <%= display_buttons(event) %>
      </div>
    </div>
  </div>
  <% end %>